# emstum ui service with 3 set of replica
apiVersion: v1
kind: Service
metadata:
  name: conf-manager-service
  namespace: cf
spec:
  selector:
    app: conf-manager-pod
  ports:
    - name: http
      protocol: TCP
      port: 7801
      targetPort: 7801
  type: LoadBalancer