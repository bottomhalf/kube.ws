# emstum ui service with 3 set of replica
apiVersion: v1
kind: Service
metadata:
  name: conf-web-service
  namespace: cf
spec:
  selector:
    app: conf-web-pod
  ports:
    - name: http
      protocol: TCP
      port: 7800
      targetPort: 80
  type: LoadBalancer