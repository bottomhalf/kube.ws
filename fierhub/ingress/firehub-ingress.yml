# ingress direct access use this for subdomain also
# port number range for emstum services start from 8201 - 8300
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fierhub-ingress
  namespace: fb
  annotations:
    cert-manager.io/cluster-issuer: "lets-encrypt"
    ingress.kubernetes.io/use-regex: "true"
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
spec:
  ingressClassName: public
  tls:
    - hosts:      
      -  www.fierhub.com
      secretName: fierhub-tls
  rules:           
  - host: www.fierhub.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: fierhub-service
            port:
              number: 8502

      - path: /api/
        pathType: Prefix
        backend:
          service:
            name: fierhub-core-service
            port:
              number: 8501              