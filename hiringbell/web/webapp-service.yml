
apiVersion: v1
kind: Service
metadata:
  name: webapp-service
  namespace: hb
spec:
  selector:
    app: webapp-pod
  ports:
    - protocol: TCP
      port: 8105
      targetPort: 80
  type: LoadBalancer
