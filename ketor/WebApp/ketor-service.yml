# ketor ui service with 3 set of replica
apiVersion: v1
kind: Service
metadata:
  name: ketor-service
  namespace: kt
spec:
  selector:
    app: ketor-pod
  ports:
    - name: http
      protocol: TCP
      port: 8502
      targetPort: 80
  type: LoadBalancer